#include <stdio.h>
#include <string.h>
#include <math.h>


int main() {
    int N,digits=0;
    scanf("%d", &N);
    if(N==0)
        digits++;
    else{
    while(N!=0){
       N=N/10;
       digits = digits+1;
   } } 
    printf("%d", digits);
    return 0;
}
