#include <stdio.h>
int main() {
    int N;
    scanf("%d", &N);
    int arr[105];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    int pos;
    scanf("%d", &pos); 
    if(pos < 1 || pos > N) {
        printf("Invalid position");
        return 0;
    }
    for(int i = pos - 1; i < N - 1; i++) {
        arr[i] = arr[i + 1];
    }
    for(int i = 0; i < N - 1; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
