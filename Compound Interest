#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int P,T;
    float R,CI,Final_Amt;
    scanf("%d", &P);
    scanf("%f", &R);
    scanf("%d", &T);
    Final_Amt=P*(pow((1+R/100),T));
    CI = Final_Amt-P;
    printf("%.2f", CI);
    
    return 0;
}
