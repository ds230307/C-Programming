#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

      int N , sum=0, digit;
      scanf("%d", &N);
      while(N!=0){
        digit=N%10;
        sum=sum+digit;
        N=N/10;  
       
    }
    printf("%d", sum); 
    return 0;
}
