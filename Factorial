//WAP to print the factorial of given numbers;
#include<stdio.h>
int main(){
    int i, n , factorial=1;
    printf("Enter the value of N");
    scanf("%d", &n);
    for(i=1; i<=n; i++){
         factorial*= i; 
       
    }
    printf("Factorial=%d\t", factorial); 
    return 0;
}
