// WAP to extract all the digits of a given number and display them
#include<stdio.h>
int main(){
    int x,n;
    printf("Enter the number=", n);
    scanf("%d", &n);
    while(n>0){
        x=n%10;
        printf("%d\n", x);
        n=n/10;

    }
    
    return 0;
}
