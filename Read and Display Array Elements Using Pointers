#include <stdio.h>
int main() {
    int N;
    int *pt1 = &N;
    scanf("%d", pt1);
    int arr[100];
    
    for(int i = 0; i < *pt1; i++) {
        scanf("%d", (arr + i));
    }
    for(int i = 0; i < *pt1; i++) {
        printf("%d ", *(arr + i));
    }

    return 0;
}
