#include <stdio.h>
int main() {
    int R, C;
    scanf("%d %d", &R, &C);
    int sum = 0;
    for(int i = 0; i < R; i++) {
        for(int j = 0; j < C; j++) {
            int val;
            scanf("%d", &val);
            sum += val;
        }
    }
    printf("%d", sum);
    return 0;
}
