#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    int arr[n], freq[n];
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
        freq[i] = 0;  
    }
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            if(arr[i] == arr[j]) {
                freq[i]++;
            }
        }
    }
    int found = 0;
    for(int i = 0; i < n; i++) {
        if(freq[i] == 1) {
            printf("%d ", arr[i]);
            found = 1;
        }
    }
    if(!found) {
        printf("No unique elements");
    }
    return 0;
}
